{
  "name": "ddd-authoring",
  "version": "0.1.0",
  "description": "DDD workflow authoring and publish pipeline",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "test": "vitest run",
    "build:tooling": "vite build",
    "build": "pnpm run build:tooling && node dist/tooling/publish/build-publish.mjs",
    "test:pack": "npx skills add ./.publish/ddd-workflow --list",
    "deploy": "pnpm test && pnpm run build && pnpm run test:pack && node dist/tooling/deploy/deploy-local.mjs",
    "deploy:dry-run": "pnpm test && pnpm run build && pnpm run test:pack && node dist/tooling/deploy/deploy-local.mjs --dry-run",
    "deploy:check": "pnpm test && pnpm run build && node dist/tooling/deploy/deploy-local.mjs --home-dir /tmp/ddd-workflow-deploy-check --skip-skills",
    "publish:status": "git -C .publish/ddd-workflow status --short",
    "publish:diff": "git -C .publish/ddd-workflow diff",
    "setup:hooks": "node scripts/setup-githooks.mjs"
  },
  "dependencies": {
    "gray-matter": "^4.0.3"
  },
  "devDependencies": {
    "vite": "^8.0.5",
    "vitest": "^4.1.2"
  }
}
