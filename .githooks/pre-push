#!/usr/bin/env sh
remote_name="$1"
hook_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
repo_root=$(CDPATH= cd -- "$hook_dir/.." && pwd)
cd "$repo_root" || exit 0
node scripts/subtree-status.mjs --hook pre-push --remote "$remote_name"
